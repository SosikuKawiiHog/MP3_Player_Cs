<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MP3_Player_Cs.MainPage"
             BackgroundColor="Black">

    <Grid RowDefinitions="*,Auto,Auto"
          ColumnDefinitions="2*,1*,4*,1*,2*"
          Padding="10">

        <!-- Playlists -->
        <StackLayout Grid.Row="0" Grid.Column="0" BackgroundColor="#242323" Padding="10">
            <Label Text="Плейлисты" TextColor="White" FontSize="24"/>
        </StackLayout>

        <Grid Grid.Row="0" Grid.Column="2">
            <Border MinimumWidthRequest="150"
                MinimumHeightRequest="150"
                MaximumWidthRequest="600"
                MaximumHeightRequest="600"
                StrokeThickness="0"
                HorizontalOptions="Center"
                VerticalOptions="Center">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="15" />
                </Border.StrokeShape>
                <Image Source="swag.png"
               Aspect="AspectFit" />
            </Border>
        </Grid>

        <!-- Queue -->
        <StackLayout Grid.Row="0" Grid.Column="4" BackgroundColor="#242323" Padding="10">
            <Label Text="Очередь" TextColor="White" FontSize="24" Margin="0,0,0,10"/>
        </StackLayout>

        <Slider Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5" />

        <!-- PlayerBar -->
        
        <Grid Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="3"
          BackgroundColor="#333" Padding="12"
          ColumnDefinitions="62,*,Auto,*,62,Auto,Auto"
          HeightRequest="80"        
          HorizontalOptions="Center" VerticalOptions="Center"
          >
            <Border MinimumWidthRequest="50"
                MinimumHeightRequest="50"
                MaximumWidthRequest="200"
                MaximumHeightRequest="200"
                StrokeThickness="0"
                HorizontalOptions="Center"
                VerticalOptions="Center">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="5" />
                </Border.StrokeShape>
                <Image Source="swag.png"
               Aspect="AspectFit" />
            </Border>
            <StackLayout Grid.Column="1" VerticalOptions="Center" Margin="0,0,10,0">
                <Label Text="Название" TextColor="White" FontSize="14" FontAttributes="Bold"/>
                <Label Text="Исполнитель" TextColor="Gray" FontSize="12"/>
            </StackLayout>

            <HorizontalStackLayout Grid.Column="2" 
                         HorizontalOptions="Center" VerticalOptions="Center"
                         Spacing="5">
                <Button ImageSource="prev_track.png" />

                <Button ImageSource="button_play.png"/>
                <Button ImageSource="next_track.png"/>
            </HorizontalStackLayout>

            <HorizontalStackLayout Grid.Column="4" Grid.ColumnSpan="2"
                          HorizontalOptions="Center"
                         VerticalOptions="Center" Spacing="15" Margin="10,0,0,0">
                <Label Text="1x" TextColor="White" VerticalOptions="Center"/>
                <Button ImageSource="volume_icon.png"/>
                <Slider Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5" />
            </HorizontalStackLayout>
        </Grid>
    </Grid>
</ContentPage>
